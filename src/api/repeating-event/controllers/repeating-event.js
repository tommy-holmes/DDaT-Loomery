'use strict';

/**
 * repeating-event controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::repeating-event.repeating-event');
